---
title: "息壤V9-部署与分发"
version: 9.2.1
status: 正式
type: 技术文档
created: 2026-05-28
updated: 2026-05-31
author: claudian
tags: [方法论, 息壤, V9, 部署, prompt-build]
maturity: stable
---

# 息壤 V9 部署与分发

> V9 合规内容如何从单一源文件分发到 7 个 Agent 平台。

## 1. 核心工具

```bash
python3 .prompt-src/prompt-build.py [--apply] [--apply-block] [--verify]
```

## 2. 源文件结构

```
.prompt-src/
├── v9-compliance-block.md      # V9 合规内容（唯一真相源）
├── preflight-auto-template.md  # 自动平台 pre-flight 模板
├── prompt-base.md              # 通用基类（路径/路由/工具/约束）
├── prompt-build.py             # 构建脚本
└── agents/
    ├── claudian.delta.md       # Claudian 特化配置
    ├── qingmeisu.delta.md      # 青霉素特化配置
    ├── hongmeisu.delta.md      # 红霉素特化配置
    └── workbuddy.delta.md      # WorkBuddy 特化配置
```

## 3. 目标平台

### 3.1 自动平台（--apply 全文覆写）

| Agent | 目标文件 | 平台 |
|-------|---------|------|
| Claudian | `.claude/agents/claudian.md` | Claude Code |
| 青霉素 | `.claude/agents/qingmeisu.md` | Claude Code |
| WorkBuddy | `.claude/agents/workbuddy.md` | Claude Code |
| 红霉素 | `.codex/instructions.md` | Codex |

生成公式：`prompt-base.md` + 注入 `v9-compliance-block.md` + 渲染 `preflight-auto-template.md` + `{agent}.delta.md`

### 3.2 手动平台（--apply-block 仅替换标记区）

| Agent | 目标文件 | 平台 |
|-------|---------|------|
| 头孢 | `~/.hermes/SOUL.md` | Hermes |
| 阿莫西林 | `~/.openclaw/workspace/AGENTS.md` | OpenClaw |
| 全局 | `.claude/CLAUDE.md` | Claude Code 项目配置 |

替换范围：`<!-- V9-COMPLIANCE-BLOCK-START -->` 到 `<!-- V9-COMPLIANCE-BLOCK-END -->` 之间的内容。
手动平台的 pre-flight 格式各自维护（使用平台特定脚本/API）。

## 4. 模板变量

| 变量 | 说明 | 示例 |
|------|------|------|
| `{{AGENT_ID}}` | Agent 英文 ID | `claudian` |
| `{{AGENT_CN}}` | Agent 中文名 | `Claudian` |
| `{{STATUS_PATH}}` | 状态文件目录 | `02-项目管理/智能体状态` |
| `{{KANBAN_PATH}}` | 看板路径 | `00-MOC/多智能体协作看板.md` |

## 5. 操作流程

### 5.1 修改合规规则

```bash
# 1. 编辑源文件
vim .prompt-src/v9-compliance-block.md

# 2. 应用到自动平台
python3 .prompt-src/prompt-build.py --apply

# 3. 应用到手动平台
python3 .prompt-src/prompt-build.py --apply-block

# 4. 验证全部一致
python3 .prompt-src/prompt-build.py --verify
```

### 5.2 新增 Agent

1. 创建 `.prompt-src/agents/{new_id}.delta.md`
2. 在 `prompt-build.py` 的 `TARGETS` dict 添加条目
3. 运行 `--apply`

### 5.3 废弃 Agent

1. 将条目从 `TARGETS` 移入 `DEPRECATED`
2. 目标文件不再更新（保留历史内容）

## 6. 内容 Hash 机制

生成文件头部：
```
<!-- AUTO-GENERATED by prompt-build.py @ v{hash} -->
```

- Hash 基于生成内容（不含头部本身）的 SHA256 前 8 位
- 不含日期 → 内容不变时 hash 不变 → 无噪音 diff
- `--verify` 通过比较当前文件内容（去头）与重新生成的内容判定是否漂移

## 7. 验证失败处理

```bash
$ python3 .prompt-src/prompt-build.py --verify
# 如果输出 DRIFT DETECTED:

# 选项 A: 接受源文件变更，重新应用
python3 .prompt-src/prompt-build.py --apply --apply-block

# 选项 B: 检查手动修改是否有意
git diff .claude/agents/  # 查看具体变更
```

---

## 8. GitHub 仓库架构

> V9.2.1 起统一为三仓库结构。所有 Agent 共知此布局。

### 8.1 仓库一览

| 仓库 | 地址 | 用途 | 本地路径 |
|------|------|------|----------|
| **xirang** | `github.com:disturbo/xirang` | 息壤 V9 公开文档站（GitHub Pages） | `~/Desktop/xirang` |
| **xirang-starter-Vualt** | `github.com:disturbo/xirang-starter-Vualt` | Starter Vault — 新用户 Clone 即用骨架 | `~/Desktop/xi-rang-v9-starter` |
| ~~xirang-dist~~ | ~~已废弃~~ | ~~旧基建同步镜像~~ | ~~已删除 2026-05-31~~ |

### 8.2 各仓库职责

#### xirang（公开文档站）

```
disturbo.github.io/xirang/
├── index.html          # 首页（核心理念/二元触发器）
├── architecture/       # 四层架构
├── workflow/           # 六步流程
├── agents/             # 6 原子 Agent
├── rules/              # 合规框架
├── deploy/             # L0-L3 部署
├── cost/               # 成本治理
└── assets/             # 共享 CSS/JS
```

- 部署方式：push to `main` → GitHub Pages 自动部署
- 更新频率：跟随 V9 版本发布
- 谁维护：Claudian

#### xirang-starter-Vualt（Starter Vault）

```
Clone 后即为可运行 Obsidian Vault：
├── .claude/            # Claude Code 配置
├── .codex/             # Codex 配置
├── .standards/         # 运行时工具链
├── .prompt-src/        # prompt-build 单源分发
├── .skills/            # Skill 路由
├── 00-MOC/             # 导航入口
├── 10-项目/            # 项目空间（空骨架）
├── 20-资料/            # 资料空间（空骨架）
├── 30-规范/            # 规范文档
├── 40-决策/            # 决策记录（空骨架）
├── 50-经验/            # 方法论 + Agent 协作文档
├── 60-归档/            # 归档（空骨架）
├── 90-模板/            # Obsidian 模板
├── README.md           # 使用说明（3分钟快速开始）
└── setup.sh            # 环境检查脚本（L2+）
```

- 面向：新用户 / 新项目 / 团队复制
- 特点：已泛化（无个人路径/项目特定规则）、有引导文档
- 更新方式：手动从 Vault 精调后 commit + push

### 8.3 更新流程

```
Vault（唯一真相源）
  │
  ├──[手动精调]──→ xirang-starter-Vualt（对外分发）
  │                  - 移除个人路径
  │                  - 移除项目特定规则
  │                  - 保留空目录骨架
  │
  └──[内容提炼]──→ xirang（文档站 HTML）
                     - 从方法论文档提炼要点
                     - 转为多页 HTML 站点
```

### 8.4 废弃说明

| 旧组件 | 废弃日期 | 原因 | 替代 |
|--------|---------|------|------|
| xirang-dist | 2026-05-31 | 机械同步无泛化、remote 冲突 | xirang-starter-Vualt |
| xirang-pages（本地clone） | 2026-05-31 | 与 xirang 源码重复 | 直接用 xirang 源码仓库 |
| sync-to-dist.sh | 2026-05-31 | 目标仓库已废弃 | 手动精调流程 |
