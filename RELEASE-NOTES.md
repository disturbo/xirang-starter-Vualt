# 息壤 V9.7.0

V9.7.0 使用一个完整 Vault 同时服务第一次使用和已有工作区升级。

## 主要变化

- ZIP 根目录直接显示可读知识库结构，可在 Obsidian 中打开使用。
- 同一个包的隐藏分发层自动判断新装、升级、校验修复或停止求助。
- 随包提供 Things 主题、3 个通用 CSS、13 个通用 Obsidian 插件和 20 个可移植 Skill；不包含息壤专用 Obsidian 插件，个人插件数据、账号和项目 Skill 不进入发行物。
- 同包提供常见宿主原生入口模板、WorkBuddy 适配、通用模板和统一根约束规范；平台注册表开放扩展，不构成固定 Agent 名单。当前 Agent 自识别并只应用自己的入口。
- SQLite StateStore 成为任务、租约、阶段、证据与验收的唯一权威状态。
- 自然语言授权绑定不可变执行包络，不绑定具体 Agent。
- 任务卡、状态页和 JSONL 只作为单向投影。
- 写前范围、逐文件 pre-image、写后收据、精确交付和他人验收形成闭环。
- 安装和升级具备持久 Journal、自动回滚、幂等重跑与篡改拒绝。
- M3/M4/M5、固定六 Agent、二元触发器、旧 Gate/心跳、Phoenix/GBrain 必选链从现行最小闭包移除。

## 已知边界

- 当前正式支持 macOS；Windows/Linux 尚未完成同等验证。
- 安装后平台入口处于“已配置，待当前会话 canary”，不得直接显示为可信接通。
- 核心被手工定制的旧工作区需要辅助迁移。
