# 息壤 V9.7.2

V9.7.2 继续使用 `xi-rang-v{version}-starter.zip` 命名结构，一个完整 Vault 同时服务第一次使用和已有工作区升级。

当前构建状态是 `candidate`。只有具名验收方接受、源码 Commit 与 annotated Tag 固定、GitHub Release 资产及 SHA256SUMS 完成闭包后，才能改为正式发布；本地 ZIP 和官网文案不能单独证明已发布。

## 主要变化

- ZIP 根目录直接显示可读知识库结构，可在 Obsidian 中打开使用。
- 同一个包的隐藏分发层自动判断新装、升级、校验修复或停止求助。
- 新增 payload 生命周期清单：机器契约强更新，根规则受管块合并，首页、MOC、模板、教训库等知识种子只在缺失时补齐，修复升级覆盖同事知识内容的风险。
- 随包提供 Things 主题、4 个通用 CSS、16 个通用 Obsidian 插件和 20 个可移植 Skill；启用 Floating TOC 与 Supercharged Links，默认不打开侧栏标题树，不包含息壤专用 Obsidian 插件。
- 插件程序、许可证与通用首装预设分离；升级保留同事已有插件配置，个人插件数据、账号和项目 Skill 不进入发行物。
- 完整包校验增加“首次打开前精确校验”和“Obsidian 打开后安全校验”两种强度，不再把正常工作区配置变化误报为包损坏。
- 将“五阶段工作流”更名为“面向人的五步协作视图”，明确机器侧仍是十个运行阶段。
- 补齐对抗审查 schema/checker、知识库生命周期规范、飞书会议纪要与原生流程图要求，并把来源映射、链接闭包和规范语义纳入发行门禁。
- 新增普通同事工作台、通用流程图规范和通用 SVG 架构图规范；飞书采集由飞书 CLI 与路由 Skill 负责，不复制静态采集规范。
- 同包提供常见宿主原生入口模板、WorkBuddy 适配、通用模板和统一根约束规范；平台注册表开放扩展，不构成固定 Agent 名单。当前 Agent 自识别并只应用自己的入口。
- SQLite StateStore 成为任务、租约、阶段、证据与验收的唯一权威状态。
- 自然语言授权绑定不可变执行包络，不绑定具体 Agent。
- 任务卡、状态页和 JSONL 只作为单向投影。
- 写前范围、逐文件 pre-image、写后收据、精确交付和他人验收形成闭环。
- 安装和升级具备持久 Journal、自动回滚、幂等重跑与篡改拒绝。
- M3/M4/M5、固定六 Agent、二元触发器、旧 Gate/心跳、Phoenix/GBrain 必选链从现行最小闭包移除。

## 已知边界

- 当前正式支持 macOS；Windows/Linux 尚未完成同等验证。
- 安装后平台入口处于“已配置，待当前会话 canary”，不得直接显示为可信接通。
- 核心被手工定制的旧工作区需要辅助迁移。
