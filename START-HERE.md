# 开始安装或升级息壤

你不需要判断自己是新安装还是升级，也不需要运行命令。

## 只做两件事

1. 打开一个能访问本机文件的 Agent。
2. 把下面整段话复制给它。

> 请帮我安装或升级息壤。安装包就在当前文件夹。请完整读取 `AGENT-INSTALL.md`，先只读检查这台电脑、你当前所在的 Agent 平台和可能的目标工作区，不要修改任何文件。请你自己识别当前平台并查询 `templates/platforms.json`；平台不限于示例名单，未登记时使用通用工作区入口，不要让我选择平台。只应用你当前平台对应的底层约束入口。请自动判断应该新安装、升级、校验修复还是停止求助；如果目标不唯一，只问我一个最必要的问题。能够执行时，用一张简短确认卡告诉我：判断、目标、保留内容、备份位置、修改范围、当前平台入口和支持状态。等我回复“开始”后，再自动完成备份、安装或升级、自检；失败时自动恢复，并用普通人能看懂的话告诉我结果。不要让我运行命令、选择版本、选择 Agent 模板或手工合并文件。

## 不要这样做

- 不要把包内 `payload/` 手工复制进工作区。
- 不要删除或修改 `manifests/`。
- 不要用别人已经运行过的工作区覆盖自己的目录。
- 不要使用标有 V9.4.3 或 V9.5.0 的旧 ZIP。

最新版、支持平台和正式下载入口以官网 Release 信息为准。
