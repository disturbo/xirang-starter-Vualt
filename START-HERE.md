# 息壤完整包的隐藏升级入口

普通用户应从完整 Vault 根目录的 `🏠-Home.md` 和 `AGENT-SETUP.md` 开始。本页位于隐藏升级载荷中，供 Agent 处理旧工作区或激活新工作区。

## 只做两件事

1. 打开一个能访问本机文件的 Agent。
2. 把下面整段话复制给它。

> 请帮我升级息壤。升级包就在当前文件夹。请完整读取 `AGENT-INSTALL.md`，先只读检查这台电脑、你当前所在的 Agent 平台和可能的旧息壤工作区，不要修改任何文件。请自动判断旧版本和目标位置；如果目标不唯一，只问我一个最必要的问题。能够执行时，用一张简短确认卡告诉我：目标、保留内容、备份位置、修改范围和支持状态。等我回复“开始”后，再自动完成备份、升级和自检；失败时自动恢复。不要让我运行命令、选择版本、选择 Agent 模板或手工合并文件。

## 不要这样做

- 不要把包内 `payload/` 手工复制进工作区。
- 不要删除或修改 `manifests/`。
- 不要用别人已经运行过的工作区覆盖自己的目录。
- 不要使用标有 V9.4.3 或 V9.5.0 的旧 ZIP。

最新版、支持平台和正式下载入口以官网 Release 信息为准。
