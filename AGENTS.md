# 息壤 V9.7 发行源规则

本仓库是息壤统一安装与升级包的公开发行源，不是个人 Vault，也不是安装后的业务工作区。

## 不可绕过的发行规则

1. 普通用户只面对与其场景对应的一个清晰入口：第一次使用下载知识库启动包，已有工作区下载 Agent 升级包；不得让用户理解发行工程目录。
2. `payload/` 是唯一 Core 来源；两个 ZIP 只能使用同一 Core，不得复制或维护第二份实现。
3. 发布前必须通过逐文件 Manifest、泄漏扫描、篡改负例、新装、升级、幂等和恢复测试。
4. 发行物不得包含个人路径、项目资料、凭证、账号标识、会话记录、运行数据库、缓存、固定角色配置或第三方 Obsidian 插件。启动包只允许构建器生成的最小安全 Obsidian 配置。
5. 用户业务内容、项目规则和自定义资料必须保留；不能安全识别或恢复时失败关闭。
6. 构建者只能提交为待验收，不得验收自己的 Release。

## 修改与发布

- 写入前说明目标、精确范围、删除项、外部影响、恢复方式和验收方。
- 只从干净 Commit 构建；Tag、Commit、Manifest、ZIP SHA 和 Release 资产必须闭合。
- 禁止 `git add .`、`git add -A`、force-push、历史改写和从个人 Vault 直接打包。
- 官网只读取 Release Manifest，不手工维护版本号和哈希；两个下载入口必须指向同一 Core 哈希。

## 当前边界

- 正式版本：`9.7.0`。
- 当前发行只承诺 macOS；Windows/Linux 可以读取说明，但不得显示为已验证。
- GBrain、Phoenix、CodeRef、状态面板和其他平台增强均为可选模块，不进入最小发行闭包。
