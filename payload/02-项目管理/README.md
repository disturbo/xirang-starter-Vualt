# 项目管理

这里承载任务入口、交付说明和运行记录，不存放具体业务正文。

## 使用方式

- 普通项目内容放入 `10-项目/`。
- 原始资料放入 `20-资料/`。
- 重要选择写入 `40-决策/`。
- 可复用经验写入 `50-经验/`。
- 任务卡由息壤运行时生成或维护时，不要手工修改受管字段。

第一次使用不需要预先建立任务文件。把目标告诉 Agent，息壤会在需要时创建对应任务记录。
