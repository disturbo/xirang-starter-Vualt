# 息壤 V9.7 治理入口

机器契约以 `.xirang/contract/policy.yaml` 为第一真源，结构化 Agent 契约、根 `AGENTS.md`、共享协议与外置 StateStore 依次约束执行。`50-经验/Agent协作方法论/` 是完整的人类可读宪法，不是第二套状态机。

安装器只维护 Manifest 登记的基建文件和根 `AGENTS.md` 的受管区块，不删除业务内容、未知文件或 Git 历史。平台入口安装不等于当前会话已经接通；未完成 canary 时必须报告“已配置，待验证”。
