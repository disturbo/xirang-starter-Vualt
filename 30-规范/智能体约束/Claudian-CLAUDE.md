---
title: "Claudian/WorkBuddy CLAUDE.md（运行时快照）"
source: ".claude/CLAUDE.md"
snapshot_date: "2026-06-11"
platform: "Claudian（Obsidian嵌入）"
agent_id: claudian
---

> 本文件为只读快照。源文件：`.claude/CLAUDE.md`

---

# CLAUDE.md — Claudian 记忆约束

> 本文件由 Claudian 启动时自动加载。所有 Agent 在本 Vault 工作的核心约束。

<!-- V9-COMPLIANCE-BLOCK-START -->

## !! STOP — 每条消息只问一件事 !!

> **收到消息后的第一判断不是「这是几档」，而是：**
> **「这个任务会写文件吗？」**
> 不写 → 直接回复。写 → pre-flight。没有第三种情况。

### 二元触发器

| 判定 | 标志 | 动作 |
|:--:|------|------|
| **不写文件** | 回答问题/讨论方案/评审/闲聊 | 直接回复，零合规开销 |
| **写文件** | 新建或修改任何文件 | 必须执行 pre-flight（下方流程） |

> 「写文件」包括：Edit / Write / 创建新文件 / 追加内容 / 运行有副作用的脚本。
> 唯一豁免：追加运行日志一行（M3 收工）、heartbeat 更新。

### 写文件时的档位分类（事后标注）

| 档位 | 场景 | 额外要求 |
|:--:|------|------|
| M3 | 改 1 个文件，<20min | 写入声明一行即可 |
| M4 | 多文件 / 产出交付物 | 完整 pre-flight |
| M5 | 跨 Agent / L/XL 长任务 | pre-flight + 范围拆分 + 验收证据 |

### 不写文件的场景（M0-M2，无合规开销）

1. 解释概念 / 纯问答 / 方法论讨论
2. 讨论方案（未到执行）/ 头脑风暴
3. 评审 / 比较 / 核对但不落盘
4. 日常闲聊 / 询问下一步
5. 巡检无异常 / 状态查询

### 灰区判定

> - 有具体对象 + 明确产出物 → **写文件，M4 pre-flight**
> - 只有咨询语气（"怎么推进"/"能不能做"）→ **不写文件，直接回复**
> - 无法判定 → **先问用户确认产物和范围**，1 轮内确认写文件意图再升 M4

### M4/M5 的 pre-flight 流程（握手格式）

```
第一步（判定为 M4/M5 后执行）：
  1. 确定自己的 agent_id
  2. 更新 02-项目管理/智能体状态/{中文名}.md — status -> busy, current_task -> 任务名
  3. 追加 task_start 事件到 02-项目管理/智能体状态/智能体事件.jsonl

第二步：在回复开头输出写入声明（无此输出 = V9 未激活）：

  M4 格式：
  V9 已激活：
  - 档位：M4
  - 任务：{任务名}
  - 写入范围：{允许写入的路径列表}
  - 正式路径：{最终产物位置，或"暂不写入"}
  - 验收方：{主Agent / 用户 / 具体Agent名}

  M5 格式（增加拆分和验收链）：
  V9 已激活：
  - 档位：M5
  - 任务：{任务名}
  - 写入范围：{路径}
  - 拆分计划：{子任务数} 个子任务
  - 验收方：{验收链}

第三步：确认三要素（项目、路径、产物类型）+ 建卡/更新看板（M4必须，M5必须）。

第四步：开始执行任务。

任务完成后（收工三件套，缺任一不得标 completed）：
  1. 产物清单：列出所有写入文件及一句话描述
  2. 自查结果：路径合规/frontmatter/无装饰性emoji/无越权声明/看板
  3. 验收建议：验收方+检查点+风险假设+待确认
  4. 更新状态文件 status -> idle, current_task -> null
  5. 追加 task_end 事件
  6. M4: 有下游写 Handoff；M5: 必写 Handoff
```

### M3 的轻量收工

```
写入声明（一行，任务开始时输出）：
  V9 写入声明：{路径} | {动作摘要}

收工（追加一行到 02-项目管理/运行日志/YYYY-MM-DD.md）：
  - HH:MM | M3 | 动作摘要 | 结果 | 产物路径(可选)

不更新状态文件，不写 event.jsonl（除非升档为 M4）。
```

### 激活判定铁律

> **唯一规则：写了文件但没声明 = 违规。**
>
> 补充：
> - 闲聊不是任务——别把日常对话变成工单。
> - `submitted` 不等于 `accepted`：未经验收方确认，不得声称"已验收"。
> - 执行中发现要写更多文件 → 暂停、补充声明、再继续。
> - 每次写入前自问：「我声明过要写这个文件吗？」没有 → 先声明。

<!-- V9-COMPLIANCE-BLOCK-END -->

---

## 铁律

1. **看板必记** — M4/M5 任务启动/完成必须在 [[多智能体协作看板]] 登记。完成任务写 Handoff。
2. **文件系统为准** — 看板可能过时，以 `ls`/`grep`/实际文件为准。
3. **禁止跨 Vault** — 只读写 `obsidianVault/`，禁止扫描 `pmpVault/` 或全桌面。
4. **原型必推送** — 修改 `沙箱/示例项目项目/示例项目EXAMPLE/prototype/` 后必须 git push 到 `origin/main`（仓库：`disturbo/EXAMPLE`）。格式：`feat|fix|refactor(模块): 描述`。

## 协作入口

- 看板：[[多智能体协作看板]]
- Home：[[🏠-Home]]
- 规范：`30-规范/README.md`

## 输出约束

- 中文优先；产出带 frontmatter
- 禁止装饰性 emoji（功能性符号 ✅⚡🐾 保留）
- Markdown 遵循 [[Markdown文档输出规范]]
- 产出后自问：emoji/frontmatter/品牌色/路径 是否合规？
